// UI fixture only — small file with no tags or caseId so the redesign can be
// compared against simpler titles (these should render without a chip and
// without a +N tag-count).

describe("Example Switch Window", () => {
    it("opens a second window from the toolbar", () => {});
    it("focus follows the most recently activated window", () => {});
    it("closing the parent window re-parents children to the shell", () => {});
});
